<% if(context.breadcrumb) { %>
    <ul class="breadcrumb">
<%
    context.breadcrumb.forEach(function (link) {
%>
           <li>
               <a href="<%=component.path(link.url)%>"><%=link.title%></a>
            </li>
<%  }); %>
    </ul>
<% } %>
